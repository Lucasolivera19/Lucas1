/*Hacer un programa que, utilizando vectores, cargue 10 números 
enteros ingresados por el usuario e imprima en pantalla la sumatoria de ellos.*/

#include <stdio.h>
#include <string.h>
#define NUMEROS 10
main()
{
	int numeros[10],n;
	int suma=0;
	
	printf("Ingresar 10 numeros:\n");
	for(int i=1;i<=10;i++)
	{
		scanf("%i",numeros);
		suma+=numeros[n]; 
	}
	
	printf("La suma de los numeros es: %i",suma);
    return 0;

}
